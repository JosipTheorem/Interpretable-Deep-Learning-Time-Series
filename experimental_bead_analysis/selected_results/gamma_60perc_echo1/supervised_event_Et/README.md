# Supervised Event E(t) Notebook Outputs

These files were extracted from the embedded outputs at the end of the original exploratory notebook:

`C:/Users/dujme/Desktop/Seminar_venv/DCIts/examples/DCIts_mulitple_run_one_echo.ipynb`

They correspond to the notebook section labelled `E(t)`, where DCIts inputs `raw_xy`, `rel_xy`, and `dxy` were trained in a supervised way to predict a binary event indicator.

The original `RezultatiV2/drugi_opt_problem/E(t)` folder was empty, so these figures come from the notebook output cells themselves.
