# Supervised Event-Prediction Figures

These figures summarize a supervised event-prediction analysis. DCIts receives `raw_xy`, `rel_xy`, or `dxy` inputs and predicts a binary event indicator derived from the motion data.

The folder contains representative figures and extracted notebook outputs for inspection.
